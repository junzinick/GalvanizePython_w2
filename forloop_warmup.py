my_list = ['hello', 'there', 'python', list('list'), '!']
for index, item in enumerate(my_list):
    if index % 2 != 0:
        index + 1
        print(index, item)
    if len(item) > 4:
        longer_elements = []
        item = longer_elements


        print(index, item)
        print(longer_elements)
print(longer_elements)
